name: deployml
summary: A simple deployment solution that works.
description:
  DeploYML is a simple deployment solution that uses a single YAML file,
  Git and SSH.

license: MIT
authors: Postmodern
email: postmodern.mod3@gmail.com
homepage: https://github.com/postmodern/deployml
has_yard: true

required_ruby_version: ">= 1.8.6"

dependencies:
  addressable: ~> 2.2.0
  rprogram: ~> 0.2
  thor: ~> 0.14

development_dependencies:
  rubygems-tasks: ~> 0.1
  rspec: ~> 2.4
  yard: ~> 0.7.0
